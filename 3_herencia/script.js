
class Largometraje{
    constructor(titulo, duración){
        this.title = titulo;
        this.duration = duración;
    }
}

const Largometraje_1 = new Largometraje ("Shrek", 130);

return `${this.title} dura ${this.duration} minutos`;

console.log(Largometraje_1.title());

class Pelicula extends Largometraje{
    constructor(){
        super(title)
    }
}

class Documental extends Largometraje{

}

class Cine extends Largometraje{
    ....
}

reproducir() {
    
  }

Cine.reproducir(pelicula);

Cine.reproducir(documental);